version: "3"
services:

  hnn:
    image: jonescompneurolab/hnn
    container_name: hnn_container
    hostname: hnn-container
    ports:
      - "22"
    environment:
      XAUTHORITY: "/tmp/.Xauthority"
      SYSTEM_USER_DIR: $HOME
    volumes:
      - "$HOME/hnn_out:$HOME/hnn_out"
